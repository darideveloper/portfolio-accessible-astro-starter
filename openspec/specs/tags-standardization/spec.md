# tags-standardization Specification

## Purpose
TBD - created by archiving change standardize-project-tags. Update Purpose after archive.
## Requirements
### Requirement: Standardize tags in project frontmatter
All project files in `src/content/projects/` SHALL use the approved SEO-optimized taxonomy for tags.

#### Scenario: Update aft.mdx
Given the project `aft.mdx`
When the tags are updated
Then they should be `['Web Development', 'Django', 'Data Analysis', 'PDF Reports']`

#### Scenario: Update c4empresarialyresidencial.mdx
Given the project `c4empresarialyresidencial.mdx`
When the tags are updated
Then they should be `['Full Stack Development', 'Next.js', 'Django', 'Security', 'Multi-page Website']`

#### Scenario: Update cancunconcierge.mdx
Given the project `cancunconcierge.mdx`
When the tags are updated
Then they should be `['Web Development', 'Astro', 'React', 'Tourism']`

#### Scenario: Update guiadeinmigracion.mdx
Given the project `guiadeinmigracion.mdx`
When the tags are updated
Then they should be `['Web Development', 'Legal Services', 'Immigration', 'Multi-page Website']`

#### Scenario: Update imimcep.mdx
Given the project `imimcep.mdx`
When the tags are updated
Then they should be `['Web Development', 'Landing Page', 'Healthcare', 'Aesthetic Medicine', 'WordPress']`

#### Scenario: Update itzimnarealestate.mdx
Given the project `itzimnarealestate.mdx`
When the tags are updated
Then they should be `['Full Stack Development', 'Next.js', 'Django', 'Real Estate', 'Dashboard']`

#### Scenario: Update iuraglobal.mdx
Given the project `iuraglobal.mdx`
When the tags are updated
Then they should be `['Web Development', 'Legal Services', 'WordPress', 'Divi']`

#### Scenario: Update leadforward.mdx
Given the project `leadforward.mdx`
When the tags are updated
Then they should be `['Web Development', 'WordPress', 'Divi']`

#### Scenario: Update mexico-real-fam-restaurant.mdx
Given the project `mexico-real-fam-restaurant.mdx`
When the tags are updated
Then they should be `['Web Development', 'Restaurant', 'Food & Beverage', 'WordPress', 'Divi']`

#### Scenario: Update moonhouse.mdx
Given the project `moonhouse.mdx`
When the tags are updated
Then they should be `['Web Development', 'Landing Page', 'Restaurant', 'Astro', 'React']`

#### Scenario: Update mundo-indomita.mdx
Given the project `mundo-indomita.mdx`
When the tags are updated
Then they should be `['Web Development', 'Multi-page Website', 'WordPress', 'Divi', 'Travel Agency']`

#### Scenario: Update opocosocial-canarias.mdx
Given the project `opocosocial-canarias.mdx`
When the tags are updated
Then they should be `['Full Stack Development', 'Django', 'Learning Platform', 'Social Work']`

#### Scenario: Update priority-traning.mdx
Given the project `priority-traning.mdx`
When the tags are updated
Then they should be `['Web Development', 'Landing Page', 'Training', 'Security']`

#### Scenario: Update project-01.mdx.temp
Given the project `project-01.mdx.temp`
When the tags are updated
Then they should be `['Accessibility', 'Smart Home', 'IoT']`

#### Scenario: Update razzer-buccarelli.mdx
Given the project `razzer-buccarelli.mdx`
When the tags are updated
Then they should be `['Web Development', 'Landing Page', 'Music', 'Art']`

#### Scenario: Update theta-pro-academy.mdx
Given the project `theta-pro-academy.mdx`
When the tags are updated
Then they should be `['Web Development', 'Landing Page', 'Trading', 'Finance']`

#### Scenario: Update xymale-barbershop.mdx
Given the project `xymale-barbershop.mdx`
When the tags are updated
Then they should be `['Web Development', 'Landing Page', 'Booking System']`

